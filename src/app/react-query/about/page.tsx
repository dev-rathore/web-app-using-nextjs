import React from "react";

const AboutReactQuery: React.FC = () => {
  return (
    <div>
      <h4 className="text-2xl font-semibold">About React Query</h4>
      <p>Also known as TanStack Query.</p>
      <p>React Query provides features like client side caching, error handling, and refetching the data.</p>
    </div>
  );
}

export default AboutReactQuery;
